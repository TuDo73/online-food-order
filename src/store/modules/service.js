// import { globalFunction } from "@/global/global.js";

const state = {
  // data
};
const getters = {
  // computed
};

const actions = {
  // methods
};

const mutations = {
  // handle response from actions to update state
};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
